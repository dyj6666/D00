# -*- coding: utf-8 -*-
"""content_part1.py —— OTA 说明书内容模块 1：基础 / 架构 / 存储 / 包格式 / 安全"""

SECTIONS = [
# =====================================================================
("h1", "第 0 章  OTA 与嵌入式基础（新手必读）"),
("p", "本章为零基础读者铺垫理解本说明书所需的全部背景知识：什么是固件、什么是 OTA、"
     "STM32 的 Flash 与启动机制、以及密码学基础。已有经验者可直接跳到第 1 章。"),

("h2", "0.1  什么是固件？什么是 OTA？"),
("pb", "固件（Firmware）：", "烧录在微控制器（MCU）内部 Flash 中的机器码程序，是设备运行的"
     "“操作系统 + 应用程序”。本平台的固件即 APP（业务与界面）与 BOOT（引导与升级）两个镜像。"),
("pb", "OTA（Over-The-Air）：", "“空中下载”，泛指不通过烧录器、而通过网络/总线把新固件传到设备并"
     "完成升级的方式。本系统的“空气”是 UART 串口、以太网（TCP/HTTP）、CAN 总线。"),
("pb", "为什么不用烧录器：", "现场设备成千上万，逐一开盖接烧录器不现实；OTA 支持远程、批量、"
     "业务不中断地升级——但代价是必须把“烧录器做的事”（擦除/写入/校验）与“信任保障”（防篡改）"
     "全部在设备内软件化实现——这正是本说明书的主体。"),
("note", "核心心智模型：OTA = 安全地把新固件从“传输通道”搬到“Flash 运行区”，并保证任何一步失败都能恢复。"),

("h2", "0.2  STM32F407 的 Flash 与启动机制"),
("pb", "内部 Flash：", "1MB，按扇区组织（0-3 号扇区 16KB、4 号 64KB、5-11 号 128KB）。"
     "擦除必须按扇区、写入按字/半字；Flash 只能 1→0 翻转，写前必须擦（擦后全 0xFF）。"),
("pb", "启动流程：", "复位后 CPU 从 0x08000000 读取向量表（栈顶指针 + 复位向量）执行。"
     "因此 0x08000000 处必须是 BOOT 引导器；BOOT 校验 APP 后跳转 0x08010000。"),
("pb", "外部 Flash：", "板载 SPI NOR Flash（如 W25Q128，16MB）独立于内部 Flash，"
     "本系统用它存放下载暂存（ota_dl）与回滚备份（img_lib）——即使升级中断电，数据不丢。"),
("code",
"内部 Flash（1MB）                        外部 SPI Flash（16MB）\n"
"0x08000000  BOOT   64KB                  0x000000  ota_dl   2MB（下载暂存，单槽1MB）\n"
"0x08010000  RUN    832KB                 0x200000  img_lib  头4KB+数据832KB（回滚源）\n"
"0x080E0000  PARAM  128KB                 其余     日志/配置/用户数据"),

("h2", "0.3  密码学基础（够用即可）"),
("pb", "对称加密（AES）：", "同一个密钥加密和解密。本系统用 AES-256-CTR（计数器模式）——"
     "流式处理，适合边解密边写入 Flash；密钥由每颗芯片唯一的 UID 派生，实现“包-设备绑定”。"),
("pb", "非对称签名（ECDSA）：", "私钥签名、公钥验签。上位机用私钥对固件包签名，BOOT 用内置公钥"
     "验证——保证包来自可信来源且未被篡改。椭圆曲线 P-256 是工业标准。"),
("pb", "哈希（SHA-256）：", "把任意长度数据压缩为 32 字节指纹；内容一变指纹即变。用于摘要+签名。"),
("pb", "防重放：", "同一份合法固件包被重复提交（重放）应被拒绝——用“构建号”单调递增实现。"),
("pb", "防回滚：", "攻击者用旧版本固件降级应被拒绝——用“版本号只升不降”实现。"),
("note", "安全不靠“加密算法保密”，而靠“密钥/私钥保密”——算法全部公开（工业标准）。"),

("h2", "0.4  看门狗（Watchdog）"),
("pb", "IWDG（独立看门狗）：", "由 LSI 低速时钟驱动，一旦启动不可关闭；软件必须周期性“喂狗”，"
     "超时即复位系统——防止程序卡死。本系统 BOOT 用 128 分频（250Hz）+ 4095 ≈16.4s（覆盖整段擦写），"
     "APP 用 32 分频（1kHz）+ 4095 ≈4.1s。"),
("pb", "任务级看门狗（WDOG）：", "APP 内软件层防线：监控各任务心跳，任务停滞超时即统一错误处理"
     "（转储+BKP 持久化+软复位）。"),
("note", "Flash 擦写是毫秒~秒级长操作，期间必须喂狗（BOOT 在 SRAM 中执行的 .ramfunc 代码里喂狗）。"),

("h2", "0.5  本系统 OTA 的一句话总结"),
("p", "“多通道把加密签名固件包送进外部暂存区 → 复位进 BOOT → BOOT 校验（芯片/防重放/签名/版本）"
     "→ 备份当前固件 → 擦除 RUN → 解密写入 → 置待确认 → 重启进新 APP → 新 APP 启动确认 → 闭环完成；"
     "任何一步失败都有对应自愈路径（续传/回滚/恢复模式）。”"),

# =====================================================================
("h1", "第 1 章  系统总体架构"),
("h2", "1.1  三端职责"),
("table",
 ["端", "角色", "关键职责"],
 [
  ["BOOT", "引导与升级执行者", "启动校验、固件验证（芯片/防重放/签名/版本）、备份/擦除/解密写入、回滚与恢复、YMODEM 救援"],
  ["APP", "运行与升级服务者", "业务运行、多通道接收固件、写入暂存、触发升级、启动确认、网络配置持久化"],
  ["HOST", "打包与调度者", "加密签名打包、版本/build 管理、批量升级、阶段可视化、私钥管理"],
 ],
 [4.0, 4.5, 8.0]),
("h2", "1.2  一条升级消息的完整旅程（30 秒理解全系统）"),
("code",
"上位机 ota_tcp_cli / OTA_Tool\n"
"   │  1. 读取 version.json（version=213, build=9181）\n"
"   │  2. encrypt_and_sign：构造 ota_header_t → AES-CTR 加密 → SHA256+ECDSA 签名\n"
"   ▼\n"
"TCP :9020 ──BEGIN(version,size)──▶ APP ota_tcp_svc\n"
"   ◀────────ACK──────────────┘  ├─ Ota_Begin：擦外部 ota_dl / 恢复续传会话\n"
"TCP ──DATA(offset,240B)──▶      ├─ Ota_Data：写 ota_dl 槽\n"
"   ◀────────ACK(rx)────────┘    └─ 循环至 459204/459204\n"
"TCP ──END──▶ APP：写 PARAM UPGRADE + BKP 标志 → 复位\n"
"   ▼\n"
"BOOT：探测 ota_dl 包头 → 芯片ID→防重放→验签→版本 → 备份 RUN→img_lib\n"
"   → 擦 RUN → AES-CTR 解密写入 → 写魔数/版本 → PARAM 置 PENDING → 重启\n"
"   ▼\n"
"BOOT 跳 APP（新固件）→ APP 启动确认 ota_confirm_startup → PARAM 置 NORMAL → 闭环完成"),
("h2", "1.3  设计原则"),
("li", "同一套下载核心（Ota_Begin/Data/End），五条通道共享；新增通道仅注册一行（ota_transport 注册表）。", "通道与核心解耦："),
("li", "BOOT 只做“校验+执行”，体积最小（35KB）；业务全部在 APP。", "职责单一："),
("li", "下载/备份/擦除/写入/提交每步都可断电恢复。", "可靠优先："),
("li", "加密+签名+芯片绑定+防重放+防回滚+密钥轮换纵深防御。", "安全纵深："),

# =====================================================================
("h1", "第 2 章  存储与分区架构（升级的物质基础）"),
("h2", "2.1  为什么这样分区"),
("p", "Flash 升级的本质是“在新旧固件之间安全切换”。分区设计回答三个问题："
     "①新固件先放哪（暂存区）；②万一失败退到哪（回滚源）；③升级状态记在哪（参数区）。"),
("table",
 ["问题", "答案（分区）", "位置"],
 [
  ["新固件先放哪", "外部 ota_dl 槽（下载暂存，单槽 1MB）", "外部 Flash 0x000000"],
  ["失败退到哪", "外部 img_lib 槽（回滚源，备份上一版 RUN）", "外部 Flash 0x200000"],
  ["状态记在哪", "PARAM 参数区（boot_state/count/build_no/会话槽）", "内部 Flash 0x080E0000"],
  ["新固件跑在哪", "内部 RUN 区（832KB，方案B 扩容）", "内部 Flash 0x08010000"],
 ],
 [5.0, 8.0, 3.5]),
("note", "方案B 的来历：原设计在内部 Flash 划 BACKUP/DOWNLOAD 区，导致 RUN 仅 320KB；"
       "方案B 把回滚源与下载暂存全部外移到外部 Flash，内部仅留 BOOT/RUN/PARAM，RUN 扩至 832KB。"),
("h2", "2.2  内部 Flash 逐区详解"),
("h3", "2.2.1  BOOT（0x08000000，64KB）"),
("li", "向量表、启动状态机、升级执行（校验/备份/擦除/解密写/回滚）、YMODEM 接收、状态帧广播。", "内容："),
("li", "固化 ECDSA 公钥（新 + LEGACY 双公钥，支持密钥轮换过渡期）。", "信任根："),
("h3", "2.2.2  RUN（0x08010000，832KB）"),
("li", "APP 向量表首地址；尾部 0x080DFFF8 写魔数 0x4F54412E、0x080DFFFC 写版本号——"
        "BOOT 跳转前以此判定“APP 是否有效”。", "页脚约定："),
("li", "魔数/版本由链接脚本独立 load region（.ota_footer）保证位于固定地址。", "链接保证："),
("h3", "2.2.3  PARAM（0x080E0000，128KB）"),
("table",
 ["子区", "地址", "用途"],
 [
  ["boot_param slot0", "0x080E0000", "启动状态/计数/回滚计数/错误码/防重放 build_no"],
  ["boot_param slot1", "0x080E0400", "双份冗余（间距 1KB），CRC-32 校验选优"],
  ["网络配置 NVM", "0x080E1000（4KB）", "APP 的 net ip 持久化（32B 槽 ×128 追加写）"],
  ["OTA 会话槽", "0x080E2000", "断点续传：magic 0x4F54414D + version + total + received + crc32"],
 ],
 [5.0, 5.0, 6.5]),
("li", "boot_param 双份 + CRC（CRC 只覆盖 crc32 字段之前，避免自引用）——单份损坏可自愈。", "冗余策略："),
("li", "BOOT 升级提交后失效全部会话槽，防止旧包续传。", "提交清理："),
("h2", "2.3  外部 Flash 逐区详解"),
("h3", "2.3.1  ota_dl（0x000000，2MB，单槽 1MB）"),
("li", "运行时 OTA（TCP/HTTP/HOSTLINK）分块写入；YMODEM 兜底同样收包到该槽。", "下载暂存："),
("li", "BOOT 探测包头（magic 0x4F5441FE 且大小合理）→ 直通应用，无需重新下载。", "预下载直通："),
("h3", "2.3.2  img_lib（0x200000，头 4KB + 数据 832KB）"),
("li", "升级前将当前 RUN 全量备份到 img_lib；PENDING 回滚/自愈由 BOOT 从该槽恢复。", "回滚源："),
("li", "新固件启动确认后备份仍保留至确认点——PENDING 期间回滚源始终可用。", "保留策略："),

# =====================================================================
("h1", "第 3 章  固件包格式（逐字节讲透）"),
("h2", "3.1  包总体结构"),
("code",
"┌────────────────────────────────────────────────────────────┐\n"
"│ ota_header_t（32 字节，明文）                                │\n"
"│   magic          4B   0x4F5441FE（'OTA' 魔数）              │\n"
"│   version        4B   固件版本（如 213）——防回滚比较        │\n"
"│   firmware_size  4B   原始固件大小（明文长度）——容量校验    │\n"
"│   aes_iv        12B   AES-CTR 初始向量（+4B 零填充成 16B）   │\n"
"│   chip_id        4B   目标芯片 ID（=DBGMCU->IDCODE & 0xFFF） │\n"
"│   build_no       4B   构建号（如 9181）——防重放比较          │\n"
"├────────────────────────────────────────────────────────────┤\n"
"│ 载荷区（密文）：AES-256-CTR 加密的 APP 二进制（长度=明文长）│\n"
"├────────────────────────────────────────────────────────────┤\n"
"│ 签名区（64B）：ECDSA P-256 签名（对 头部+载荷 的 SHA256 摘要）│\n"
"└────────────────────────────────────────────────────────────┘"),
("h2", "3.2  字段逐个说明"),
("table",
 ["字段", "字节", "含义", "谁生成", "谁校验"],
 [
  ["magic", "4", "包类型标识（OTA 包 vs 其他）", "上位机", "BOOT 探测"],
  ["version", "4", "固件版本号——只升不降（防回滚）", "version.json", "BOOT（Rollback denied）"],
  ["firmware_size", "4", "原始固件大小——校验 ≤832KB（RUN 容量）", "上位机", "BOOT"],
  ["aes_iv", "12", "CTR 模式初始向量（随机/可预测均可，CTR 不重复即可）", "上位机", "BOOT 解密用"],
  ["chip_id", "4", "目标芯片 ID——防跨芯片烧录", "上位机（目标 UID）", "BOOT（Chip mismatch）"],
  ["build_no", "4", "构建号——严格递增（防重放）", "version.json（递增）", "BOOT（Replay denied）"],
 ],
 [3.0, 2.0, 6.5, 3.0, 3.5]),
("note", "防重放实战（本平台真实案例）：PARAM 持久化 last_build_no；同构建号包（build=9180=last）"
         "被 BOOT 拒绝“Replay denied”；构建号递增（9181）后立即通过。"),
("h2", "3.3  打包过程（上位机视角，逐步）"),
("code",
"1. 读 APP.bin 原始固件\n"
"2. 构造 ota_header_t：magic/version(213)/size/aes_iv(12B)/chip_id/build_no(9181)\n"
"3. derive_aes_key：由设备 UID 派生 32B AES 密钥（与 BOOT 一致）\n"
"4. AES-256-CTR 加密载荷（iv = aes_iv + 4B 零）\n"
"5. SHA256(头部 + 密文) 得 32B 摘要\n"
"6. ECDSA P-256 私钥（环境变量 OTA_PRIVKEY）对摘要签名 → 64B 签名\n"
"7. 输出：头部 + 密文 + 签名（encrypt_and_sign 完成）"),
("h2", "3.4  解包过程（BOOT 视角）"),
("code",
"1. 读外部 ota_dl 头部 → magic 校验\n"
"2. chip_id 校验 → build_no 防重放 → body_len 容量校验\n"
"3. SHA256(头部+密文) → ECDSA 双公钥验签（新/LEGACY 任一通过）\n"
"4. version 防回滚（>= 当前版本）\n"
"5. derive_aes_key（UID）→ AES-CTR 流式解密，边解密边写 RUN\n"
"6. 写魔数/版本 → PARAM 置 PENDING → 重启"),

# =====================================================================
("h1", "第 4 章  安全模型（攻击者视角的攻防推演）"),
("h2", "4.1  威胁与防线总览"),
("table",
 ["攻击手段", "攻击者目标", "防线", "失败输出"],
 [
  ["窃听传输", "拿到固件逆向", "AES-256-CTR 加密（密钥=设备 UID，跨设备无意义）", "密文无明文价值"],
  ["篡改包体", "注入恶意代码", "SHA256+ECDSA 签名（私钥不在设备/仓库）", "ECDSA verify failed!"],
  ["伪造整个包", "分发恶意固件", "签名私钥不可得；芯片 ID 绑定", "Chip mismatch!"],
  ["重放旧包", "回退到有漏洞版本", "build_no 严格递增（PARAM 持久化）", "Replay denied!"],
  ["降级回滚", "利用旧版漏洞", "version 只升不降 + 密钥轮换（旧包可能旧密钥）", "Rollback denied!"],
  ["跨设备烧录", "一个包刷全部", "chip_id == DBGMCU->IDCODE & 0xFFF", "Chip mismatch!"],
  ["读 Flash 提取", "拿固件/密钥", "RDP Level 1 读保护（发布前启用，DAP 不可读）", "调试器无法连接"],
 ],
 [3.5, 4.0, 6.5, 3.0]),
("h2", "4.2  信任根与密钥体系"),
("li", "BOOT 固化 ECDSA 公钥——设备内唯一信任锚；一切校验从公钥出发。", "信任根："),
("li", "AES 密钥 = f(芯片 UID)——包与设备绑定，无法跨设备复用。", "设备绑定："),
("li", "BOOT 固化双公钥（新 + LEGACY）——密钥轮换过渡期新旧包都能验签；成熟后移除旧公钥。", "密钥轮换："),
("li", "签名私钥仅存在于上位机环境变量（OTA_PRIVKEY），不入仓库、不出内存。", "私钥管理："),
("h2", "4.3  校验顺序与失败策略"),
("p", "BOOT 按“成本递增、尽早拒绝”顺序校验：芯片 ID（最快）→ 防重放 → 长度 → 签名（最贵）→ 版本。"
     "任一失败：打印原因、归一化参数（清除升级残留）、状态帧广播错误码、跳回 APP（支持重新下载/续传）——"
     "绝不卡死在升级模式。"),
("h2", "4.4  升级提交后的安全（启动确认与回滚）"),
("li", "新固件写入后置 PENDING；BOOT 每次启动 count++ 并跳 APP。", "待确认："),
("li", "APP 首次正常运行调 ota_confirm_startup → PENDING 转 NORMAL（防回滚闭环）。", "确认："),
("li", "新固件崩溃/未确认 → BOOT 计数到 3 → 自动回滚 img_lib 备份。", "回滚："),
("li", "回滚超限（rollback_count>=MAX，上限 5）→ RECOVERY 恢复模式；无有效包时自动归一 NORMAL 回旧固件（有意妥协：宁用旧固件不砖机），升级通道随时可恢复。", "恢复："),
]
