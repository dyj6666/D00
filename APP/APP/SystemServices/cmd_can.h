/* ================================================================
 * cmd_can —— CAN Shell 传输适配器（预留）
 *
 * 架构位置：APP 服务层；CMD_ENABLE_CAN=1 时启用
 * ================================================================ */
/* ================================================================
 * cmd_can —— CAN Shell 传输适配器（预留）
 *
 * 架构位置：APP 服务层；CMD_ENABLE_CAN=1 时启用
 * ================================================================ */
#ifndef CMD_CAN_H
#define CMD_CAN_H

/* CAN 传输适配器（未来扩展）。启用步骤见 cmd_can.c 头注释。 */
void CmdCan_Register(void);

#endif
